name: Triển khai ReactJS lên Server

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Lấy mã nguồn
        uses: actions/checkout@v4

      - name: Kiểm tra cấu trúc repo
        run: |
          echo "📁 Listing repo root:"
          pwd
          ls -la
          echo "📁 Listing fe/:"
          ls -la fe || echo "❌ Không tìm thấy thư mục fe"

      - name: Chuẩn bị file log
        run: touch ci.log

      - name: Cài Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Cài đặt thư viện FE
        working-directory: ./fe
        run: |
          npm install 2>&1 | tee -a ../ci.log

      - name: Build dự án FE
        working-directory: ./fe
        run: |
          npm run build 2>&1 | tee -a ../ci.log

      - name: Triển khai FE lên Server qua SSH
        uses: easingthemes/ssh-deploy@v4
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_KEY }}
          REMOTE_HOST: ${{ secrets.SSH_HOST }}
          REMOTE_USER: ${{ secrets.SSH_USER }}
          SOURCE: "fe/dist/"
          TARGET: "/var/www/app"

      - name: Gửi log + link GitHub Actions lên Discord
        if: always()
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          JOB_STATUS: ${{ job.status }}
        run: |
          [ -f ci.log ] || echo "No log captured." > ci.log

          RUN_URL="https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"

          if [ "$JOB_STATUS" = "success" ]; then
            ICON="✅"
          else
            ICON="❌"
          fi

          CONTENT="$ICON Kết quả CI/CD: $JOB_STATUS\nRepo: ${GITHUB_REPOSITORY}\nCommit: ${GITHUB_SHA}\nXem full log trên GitHub: ${RUN_URL}"

          echo "{\"content\":\"${CONTENT}\"}" > payload.json

          curl -sS \
            -F "payload_json=@payload.json" \
            -F "file1=@ci.log;type=text/plain" \
            "$DISCORD_WEBHOOK"